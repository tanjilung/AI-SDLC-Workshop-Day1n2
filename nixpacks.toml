[provider]
image = "node"

[phases.setup]
nixPkgs = ['python3', 'pkg-config', 'sqlite-deprecated', 'gcc', 'glibc', 'make']

[phases.dependencies]
cmds = [
  'npm install --build-from-source --prefer-offline'
]

[start]
cmd = "next start"
